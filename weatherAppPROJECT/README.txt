this application shows the current weather conditions (such as temperature, humidity, pressure,...) of a location from user
input

using data from "open weather app" API

import json library to compile the app
